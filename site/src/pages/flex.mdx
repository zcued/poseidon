import Helmet from 'react-helmet'

<Helmet
  title="Zcool UI - Flex"
  meta={[
    {
      name: 'description',
      content: 'Zcool UI - Flex'
    }
  ]}
/>

# Flex

```js
import Flex from '@zcool/flex'
```

```.jsx
<Flex
  style={{ width: 200, height: 200, backgroundColor: 'lightgray' }}
  valign='center'
  halign='center'
>
  <span>flex center</span>
</Flex>
```
