import Helmet from 'react-helmet'

<Helmet
  title="Zcool UI - Pagination"
  meta={[
    {
      name: 'description',
      content: 'Zcool UI - Pagination'
    }
  ]}
/>

# Pagination

```js
import Pagination from '@zcool/pagination'
```

```.jsx
() => {
  function App() {
    const [current, setCurrent] = React.useState(10)

    function handleChange(value) {
      setCurrent(value)
      console.log(value)
    }

    return (
      <div style={{ background: '#f0f4f5' }}>
        <Pagination current={current} total={25} pageSize={10} />
        <Pagination
          current={current}
          pageSize={10}
          total={25}
          showTotal={total => `共 ${total} 页`}
          onChange={handleChange}
        />
      </div>
    )
  }

  return <App />
}
```
